import org.junit.Test;
import static org.junit.Assert.*;

public class V2VMTest {
    @Test public void testAppHasAGreeting() {
        V2VM classUnderTest = new V2VM();
        assertEquals(true, true);
    }
}
